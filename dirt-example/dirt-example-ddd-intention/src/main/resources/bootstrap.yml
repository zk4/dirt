spring:
  application:
    name: ddd-intention

  cloud:
    nacos:
      config:
        server-addr: smallmac.local:8848
      discovery:
        enabled: true
        server-addr: smallmac.local:8848

